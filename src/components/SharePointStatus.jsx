import React from'react';import{Cloud,RefreshCw,AlertTriangle,CheckCircle2}from'lucide-react';
export default function SharePointStatus({meta,refreshing,onRefresh}){const fallback=!!meta.syncError;return <div className={'sharepointStatus '+(fallback?'fallback':'online')}>{fallback?<AlertTriangle/>:<CheckCircle2/>}<span><b>{fallback?'Usando base de contingência':'Sincronizado com SharePoint'}</b><small>{meta.arquivo||'CONTROLE_DE_REQUISICOES_2026.xlsx'} · {meta.atualizadoEmTexto||'—'}</small>{fallback&&<em>{meta.syncError}</em>}</span><button onClick={onRefresh}disabled={refreshing}>{refreshing?<RefreshCw className="spin"/>:<Cloud/>}Atualizar agora</button></div>}

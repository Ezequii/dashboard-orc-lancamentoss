import React from'react';import{Download,Save,Trash2}from'lucide-react';
export default function ProductivityBar({exportData,saveFilter,savedFilters,applyFilter,removeFilter}){return <div className="productivityBar"><div><button onClick={exportData}><Download/>Exportar CSV</button><button onClick={saveFilter}><Save/>Salvar filtro</button></div><div className="savedFilters">{savedFilters.map(filter=><span key={filter.name}><button onClick={()=>applyFilter(filter)}>{filter.name}</button><button title="Excluir filtro"onClick={()=>removeFilter(filter.name)}><Trash2/></button></span>)}</div></div>}

import test from'node:test';import assert from'node:assert/strict';import{sortOperational,cleanText}from'../src/utils/dataUtils.js';test('ordem operacional',()=>assert.deepEqual([{status:'Concluído'},{status:'Falta NF'},{status:'Falta lançamento'},{status:'Falta pedido'}].sort(sortOperational).map(x=>x.status),['Falta lançamento','Falta pedido','Falta NF','Concluído']));test('normaliza',()=>assert.equal(cleanText(' CHARLES\u00a0 SANTOS '),'CHARLES SANTOS'));
